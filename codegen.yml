schema: "src/schema.ts"
documents: "src/**/*.ts"
generates:
  src/types.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-resolvers"
    config:
      contextType: ./context#MyContext
